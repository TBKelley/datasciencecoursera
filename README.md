# The Data Scientist’s Toolbox - Peer Assessment #

Hello World.

Used to demonstate GitHub has been correctly setup.

## Create a new repository on the command line ##
touch README.md
git init
git add README.md
git commit -m "Hello world!"
git remote add origin https://github.com/TBKelley/datasciencecoursera.git
git push -u origin master

## Push an existing repository from the command line ##
git remote add origin https://github.com/TBKelley/datasciencecoursera.git
git push -u origin master
